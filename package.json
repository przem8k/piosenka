{
  "name": "piosenka-pwa-build",
  "version": "1.0.0",
  "private": true,
  "description": "Build tooling for the PWA service worker.",
  "scripts": {
    "build:sw": "node scripts/build-sw.js",
    "build:icons": "node scripts/generate-icons.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "sharp": "^0.34.0",
    "workbox-build": "^7.4.0"
  }
}
